╔══════════════════════════════════════════════════════════════════════════════╗
║              SEELOGD — TAM PROJE ANALİZ RAPORU                           ║
║              Tarih: 2026-05-17  |  Kapsam: Tüm kaynak dosyalar             ║
╚══════════════════════════════════════════════════════════════════════════════╝

Bu rapor projenin tamamı (68 ekran, 14 context, 18 bileşen, 5 modül)
taranarak hazırlanmıştır. Her bölüm somut dosya yolu ve satır numarasıyla
desteklenmiştir.

─────────────────────────────────────────────────────────────────────────────
  PROJE İSTATİSTİKLERİ
─────────────────────────────────────────────────────────────────────────────

  Toplam ekran dosyası      : 68 dosya  (~26.700 satır)
  Toplam context dosyası    : 14 dosya  (~2.900 satır)
  Toplam bileşen dosyası    : 18 dosya
  En büyük dosyalar         :
    MovieDetail.js          1.839 satır
    TvShowsDetails.js       1.837 satır
    ChatScreen.js           ~2.100 satır
    MovieSearch.js          1.241 satır
    TvShowSearch.js         1.232 satır
    ChatModal.js            ~1.500 satır (43 KB)
    ProfileScreen.js        964 satır
    SettingsScreen.js       990 satır
  Bağımlılık sayısı         : 46 paket
  Firebase onSnapshot sayısı: 18+ aktif listener (tüm proje)
  console.log olan dosya    : 29 dosya


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  BÖLÜM 1 — ✅ İYİ OLAN ŞEYLER
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1.1  Firestore Veri Modeli (Alt koleksiyonlar) ✅
     Notes/{uid}/items/{noteId}                        ← subcollection
     Reminders/{uid}/movies/{movieId}                   ← subcollection
     Reminders/{uid}/tvShows/{showId}/episodes/{epKey}  ← subcollection
     Lists/{uid}/favorites/{id}                         ← subcollection (yeni)
     Lists/{uid}/watchList/{id}                         ← subcollection (yeni)
     Lists/{uid}/watchedMovies/{id}                     ← subcollection (yeni)
     Lists/{uid}/watchedTv/{showId}/seasons/{no}        ← subcollection (yeni)
     → Tüm büyük array'ler kaldırıldı; her işlem tek doküman okuyup yazıyor.

1.2  ListStatusContext O(1) Index ✅
     context/ListStatusContext.js satır 67-116
     → statusIndex.movie[id] / statusIndex.tv[id] ile anlık liste durum sorgusu.
     → some/filter döngüsü yok, tüm listeler tek useMemo'da birleştirildi.
     → Subcollection + eski root-doc fallback mantığı birlikte çalışıyor.

1.3  TMDB API Önbellekleme ✅
     utils/apiCache.js — in-memory + AsyncStorage, TTL'li çift katmanlı cache
     → Trends: 1 saat, Providers: 24 saat, Genres: 7 gün, Oscar: 7 gün
     → CalendarContext, MovieContex, TvShowContex entegre.
     → Uygulama yeniden açıldığında TMDB'ye istek atmıyor.

1.4  ProfileScreenContext Parçalanması ✅
     4 ayrı küçük context:
     ProfileStatsContext.js, ProfileNotesContext.js,
     ProfileRemindersContext.js, ProfileUiContext.js
     → Not eklemek istatistikleri yeniden render etmiyor.

1.5  Comment Sistemi ✅
     components/Comment.js
     → Lazy reply subscription (açınca subscribe, kapanınca unsubscribe)
     → replyCount denormalized field ile sayı tutuldu
     → likeCount + likedBy map pattern ile unbounded array sorunu çözüldü
     → Batch seen/delivered write ile ChatScreen maliyeti düşürüldü

1.6  AppSettingsContext Temizliği ✅
     context/AppSettingsContext.js
     → AsyncStorage.multiGet ile 7 ayar tek round-trip'te okunuyor
     → LanguageContext, ThemeContext, SnowContext AsyncStorage'a dokunmuyor
     → Tüm ayarlar tek kaynak (AppSettingsContext)

1.7  Reminder Sistemi ✅
     context/ProfileRemindersContext.js
     → Dinamik per-show episode listener (showlar değişince listener güncelleniyor)
     → writeBatch ile migration
     → epUnsubsRef.current haritasıyla bellek sızıntısı yok

1.8  SwipeCard ✅
     modules/SwipeCard.js (207 satır)
     → react-native-reanimated + gesture-handler kullanıyor (JS thread bypass)
     → runOnJS ile güvenli callback
     → Ekranın %25'i eşiği, %15 sınır — dengeli UX

1.9  UserStats Collection ✅ (yeni eklendi)
     UserStats/{uid}: movieCount, totalMovieMinutes, genreCounts (map)
     → Film eklenince increment(), silinince decrement()
     → genreCounts dot notation ile güncelleniyor (map silinmiyor)

1.10 Online Status ✅ (yeni eklendi)
     App.js
     → auth.onAuthStateChanged + AppState.addEventListener
     → Uygulama arka plana geçince isOnline=false, ön plana gelince=true
     → Cleanup'ta updateStatus(false)


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  BÖLÜM 2 — 🔴 KRİTİK SORUNLAR (Hemen Düzeltilmeli)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

2.1  🔴 HARDCODED API KEY — GÜVENLİK AÇIĞI
     Dosya: context/AppSettingsContext.js satır ~38
     Dosya: components/ChatModal.js satır ~270
     Sorun: TMDB Bearer token ve Gemini API key kaynak kodda düz metin.
            Git geçmişinde var. Repo herkese açıksa key çalınabilir.
     Çözüm:
       1. .env dosyasına taşı:
            EXPO_PUBLIC_TMDB_KEY=Bearer eyJhbGc...
            EXPO_PUBLIC_GEMINI_KEY=AIzaSy...
       2. AppSettingsContext'te:
            const API_KEY = process.env.EXPO_PUBLIC_TMDB_KEY;
       3. ChatModal'da: fallback'i tamamen kaldır, sadece env var kullan
       4. Mevcut keylerini Google/TMDB Console'dan iptal et, yenisini al
       5. Git geçmişini temizle (BFG Repo Cleaner):
            bfg --replace-text passwords.txt
     Risk: Birinin bu key'i bulup TMDB kotanı veya Gemini API'ni
           doldurması mümkün. Ücretsiz Gemini limiti aşılabilir.

2.2  🔴 Firebase Config Açık
     Dosya: firebase.js satır 10
     Sorun: apiKey: "AIzaSyDoooqZO4X..." hardcoded
     Not:   Firebase web apiKey gizli değildir (Security Rules korur),
            ancak projectId + apiKey birleşimi kötüye kullanılabilir.
            En azından environment variable'a taşı.

2.3  🔴 Firestore Security Rules Uygulanmadı
     Durum: MASTER_GOREV_LISTESI.txt'de hazır rules var fakat
            Firebase Console'da henüz uygulanmadı.
     Risk:  Şu an tüm veriler auth edilmiş HER kullanıcıya açık olabilir.
            Başkasının listelerini, notlarını okuyabilir.
     Çözüm: Firebase Console → Firestore → Rules → Kopyala → Publish


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  BÖLÜM 3 — 🟠 PERFORMANS SORUNLARI
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

3.1  MovieContex.js — 29 State, 10 useEffect
     Dosya: context/MovieContex.js (584 satır)
     Sorun: Trends, Bests, Oscar, Collection, Providers, Genres, Upcoming,
            NowPlaying hepsi TEK context'te. Herhangi bir state değişince
            tüm consumer bileşenler re-render oluyor.
     Ölçek: 8 farklı özellik × ortalama 3-4 state = 29 state
     Çözüm: Lazy loading ile tab-bazlı fetch zaten MASTER'da planlandı (Madde 4)
            Önce: fetchMoviesOscar() uygulama açılışında çalışıyor (30 istek)
            Sonra: Tab ekranı mount olduğunda tetikle

3.2  TvShowContex.js — 23 State, 9 useEffect, 1 onSnapshot
     Dosya: context/TvShowContex.js (465 satır)
     Sorun: MovieContex ile aynı. Ek olarak:
            - `setLoadingTren` typo (d eksik) — okunabilirlik sorunu
            - Firebase + TMDB listener eş zamanlı çalışıyor
            - 9 useEffect sıralaması takip edilmesi zor

3.3  ProfileStatsContext — scaleValues state'te
     Dosya: context/ProfileStatsContext.js satır ~109, 207-210
     Sorun: scaleValues (Animated.Value) hâlâ useState içinde.
            listItems her değiştiğinde tüm Animated.Value'lar yeniden yaratılıyor.
     Çözüm: useRef'e taşı (ListView/ListViewTv'de düzeltildi, burası kaldı)

3.4  Çok Fazla Eş Zamanlı Firestore Listener
     Toplam aktif onSnapshot: 18+
     Dağılım:
       ListStatusContext    : 5 listener (root + 4 subcollection)
       CalendarContext      : 2-3 listener (notes + reminders)
       ProfileReminders     : 3+ listener (movies + shows + dynamic episodes)
       ChatScreen           : 3 listener (messages + chat doc + friend doc)
       ProfileStats         : 3 listener (root + watchedMovies + watchedTv)
       TvShowContex         : 1 listener (eski watchedTv)
       ListsScreen          : 1 listener
       ListsViewScreen      : 1 listener
       ListsTvShowScreen    : 1 listener
       hooks/useListData    : 1 listener
       MovieDetail          : 1 listener (comments)
     Risk: Pil tüketimi, Firestore okuma maliyeti

3.5  Oscar + Collection Her Açılışta Fetch
     Dosya: context/MovieContex.js satır 137-162, 195-226
     Sorun: Oscar için 30 paralel API isteği, Collection için 19 istek.
            Cache çalışıyor ama SADECE aynı oturumda. Uygulama kapanıp
            açılınca TTL (7 gün) dolmadıkça tekrar göndermiyor — bu aslında düzgün.
     Durum: Büyük ölçüde çözüldü (apiCache.js ile). Kontrol et.

3.6  İç İçe ScrollView + FlatList
     Dosyalar: screens/movie/MovieDetail.js, screens/tv/TvShowsDetails.js,
               screens/ListsScreen.js
     Sorun: Dış ScrollView içinde yatay FlatList'ler var.
            React Native bu kombinasyonda kaydırma performansını bozabilir.
     Çözüm: Dış ScrollView'u SectionList/FlatList'e dönüştür.
            Ya da nested FlatList için nestedScrollEnabled + scrollEnabled={false}

3.7  Animation valueOf Sızıntısı
     Dosyalar: screens/movie/MovieBests.js, screens/movie/MovieTrends.js
               (düzeltildi), context/ProfileStatsContext.js (kaldı)
     Sorun: Animated.Value useState içinde yaratılıyor → her data
            değişiminde GC edilemeyen nesneler birikir.

3.8  LottieView Performansı
     Dosyalar: screens/tabs/MovieScreen.js, screens/tabs/TvShowScreen.js
     Sorun: Snow aktifken 4 LottieView aynı anda GPU'da çalışıyor.
            {showSnow && ...} conditional render yapıldı ama
            birden fazla kopyası hâlâ var.
     Çözüm: 1 kopya, position:absolute overlay, tüm ekranı kapsasın.

3.9  Refresh Fonksiyonlarında Sonsuz Loading Riski
     Dosyalar: screens/tabs/MovieScreen.js satır 49-59,
               screens/tabs/TvShowScreen.js satır 40-49
     Sorun: onRefresh içinde herhangi bir fetch hata verirse
            setRefreshing(false) çağrılmıyor → kullanıcı sonsuz spinner görüyor.
     Çözüm: try/catch ile finally'de setRefreshing(false)

3.10 ProfileUiContext — 56 Avatar require() Her Açılışta
     Dosya: context/ProfileUiContext.js
     Sorun: 56 avatar fotoğrafının require() çağrısı module-level'da yapılıyor.
            Uygulama açılışında hepsi belleğe yükleniyor.
     Çözüm: Lazy load veya CDN/Cloudinary URL'leri ile değiştir.
            Şu an Cloudinary entegrasyonu var (screens/tabs/profile/UploadToCloudinary.js)
            ama avatar sistemi bunu kullanmıyor.


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  BÖLÜM 4 — 🗄️ DATABASE (FIRESTORE) SORUNLARI
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

4.1  Lists/{uid} Root Döküman Hâlâ Kullanılıyor
     Dosya: context/ListStatusContext.js, context/TvShowContex.js
     Sorun: Eski watchedTv/watchedMovies/favorites/watchList array'leri
            root dökümanda kalmaya devam ediyor.
            Yeni subcollection yazılırken eski array silinmiyor.
            Döküman boyutu gereksiz yere büyük.
     Çözüm: Bir kez çalışacak migration script yaz:
            - Subcollection boşsa eski array'den migrate et
            - Subcollection doluysa eski array alanını deleteField() ile sil

4.2  Users/{uid}.friends Array Hâlâ Doluyor
     Dosya: screens/tabs/profile/FriendRequestsScreen.js handleAccept
     Durum: Dual-write yapıyoruz (subcollection + root array)
            Ancak root array hiçbir zaman temizlenmiyor.
     Çözüm: Belirli bir süre sonra (subcollection stable olduktan sonra)
            root array'i temizle: updateDoc(userRef, { friends: deleteField() })

4.3  TvShowContex.js Hâlâ Eski onSnapshot Açıyor
     Dosya: context/TvShowContex.js satır 38-56
     Sorun: Bu listener Lists/{uid} root dökümanını dinliyor ve
            watchedTv array'ini okuyor. Yeni subcollection'a geçildi ama
            bu eski listener hâlâ aktif → fazla Firestore okuma.
     Çözüm: Bu listener'ı ProfileStatsContext'teki subcollection listener ile
            değiştir veya kaldır.

4.4  ListsScreen.js — Kendi Başına onSnapshot Açıyor
     Dosya: screens/ListsScreen.js satır 134
     Sorun: Lists/{uid} dokümanını ayrıca dinliyor.
            ListStatusContext zaten aynı dokümanı dinliyor.
            Aynı belgede 2 aktif listener = 2x okuma maliyeti.
     Çözüm: useListStatusContext().allLists kullan, kendi listener'ını kaldır.

4.5  ListsViewScreen.js ve ListsTvShowScreen.js Aynı Sorun
     Dosyalar: screens/ListsViewScreen.js satır 346
               screens/ListsTvShowScreen.js satır 35
     Sorun: 4.4 ile aynı. Üç ekran aynı belgeyi ayrı ayrı dinliyor.
     Çözüm: Her ikisini de ListStatusContext üzerinden besle.

4.6  ChatModal.js — ProfileStatsContext'e Bağımlı
     Dosya: components/ChatModal.js satır 66-72
     Sorun: Modal sadece tür özeti (genre bilgisi) için devasa
            ProfileStatsContext'e bağlı. Stats değişince modal da re-render oluyor.
     Çözüm: Genre bilgisini props olarak geçir veya ayrı hafif bir hook yaz.

4.7  hooks/useListData.js — Ayrı Listener
     Dosya: screens/search/useListStatus.js (useListData)
     Sorun: Bu dosya kendi başına Lists/{uid} dinliyor olabilir.
            useListStatusContext kullanıyor mu kontrol et.

4.8  MovieDetail.js — Comments Listener Her Detail Açılışta
     Dosya: screens/movie/MovieDetail.js satır ~291
     Sorun: onSnapshot(collection(MovieComment/{id}/comments)) her detail
            açılışında başlıyor. Geri tuşuna basılınca cleanup yapılıyor mu?
     Kontrol: useEffect return'ünde unsub() var mı?

4.9  UserStats Senkronizasyonu Eksik
     Durum: MovieDetail.js watchedMovies ekle/sil → UserStats güncelleniyor.
            Ancak eski format (root-doc array) ile eklenen filmler için
            UserStats hiç doldurulmadı → istatistikler yanlış olabilir.
     Çözüm: Bir kez çalışacak migration: mevcut watchedMovies'i okuyup
            UserStats'ı hesapla ve yaz.

4.10 N+1 Arkadaş Listesi Sorunu
     Dosyalar: FriendsListScreen.js, FriendRequestsScreen.js
     Sorun: Her arkadaşın profili için ayrı Firestore read yapılıyor.
            50 arkadaşta 50 ayrı getDoc() çağrısı.
     Çözüm: Arkadaş bilgilerini friends dokümanında denormalize tut
            (uid, displayName, username, avatarIndex) — zaten var.
            Ek bilgi gerekiyorsa sadece gerektiğinde getDoc() yap.


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  BÖLÜM 5 — 🟡 KOD KALİTESİ SORUNLARI
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

5.1  Dosya Adı Typo'ları
     context/MovieContex.js     → MovieContext.js olmalı
     context/TvShowContex.js    → TvShowContext.js olmalı
     components/IconBacground.js → IconBackground.js olmalı
     screens/movie/MovieProvders.js → MovieProviders.js olmalı
     screens/tv/TvShowsProvders.js  → TvShowsProviders.js olmalı
     Etki: IDE autocompletion bozuluyor, arama zorlaşıyor.

5.2  Değişken Adı Typo'ları
     context/TvShowContex.js: setLoadingTren (d eksik)
     context/AppSettingsContext.js: chaneAdultContent (g eksik)
     screens/auth/LoginScreen.js: isloading (büyük L eksik)
     screens/auth/RegisterScreen.js: isloading
     screens/tabs/profile/TvStatisticsScreen.js: isLoadingTv (var mı kontrol et)

5.3  29 console.error/log/warn Olan Dosya — Production'da Kalsın
     Tüm proje genelinde 29 dosyada debug log var.
     Production build'de JS thread yavaşlatır.
     Çözüm: babel.config.js'e ekle:
       plugins: [["transform-remove-console", { "exclude": ["error"] }]]
     Ya da __DEV__ ile koru:
       if (__DEV__) console.log(...)

5.4  MovieScreen.js — Kullanılmayan closeSearch Fonksiyonu
     Dosya: screens/tabs/MovieScreen.js satır 70-75
     closeSearch tanımlanmış, hiçbir yere geçirilmemiyor.

5.5  App.js — Gereksiz Duplicate Mantık
     Dosya: App.js satır ~151-175
     showChatModal hem useEffect'te hem onStateChange'de yönetiliyor.
     Biri kaldırılmalı.

5.6  @react-navigation/stack Gereksiz Bağımlılık
     Dosya: package.json satır ~20
     Sorun: @react-navigation/native-stack daha performanslı (native thread).
            @react-navigation/stack (JS thread) hâlâ yüklü.
            Gerçekten kullanılıyor mu? Kullanılmıyorsa kaldır:
              npm uninstall @react-navigation/stack

5.7  react-native-appwrite Tanımsız Override
     Dosya: package.json satır ~72-79
     Sorun: overrides bloğunda react-native-appwrite var ama
            dependencies'de bu paket yok. Ölü kod.

5.8  Google Sign-In Entegrasyonu Yarım
     Dosya: screens/auth/LoginScreen.js, package.json
     Sorun: @react-native-google-signin/google-signin yüklü ve
            LoginScreen'de kullanılıyor gibi görünüyor ama
            App.js'te GoogleSignin.configure() yok.
            iOS için info.plist, Android için google-services.json yapılandırması var mı?

5.9  react-native-markdown-display Şişirilmiş Paket
     Dosya: package.json
     Kullanım yeri: Muhtemelen ChatModal.js (Gemini yanıtları için)
     Sorun: 7.0.2 sürümü büyük bir paket. Basit markdown için alternatif:
            - Sadece bold/italic lazım: regex ile manuel render
            - Ya da expo-markdown-display (daha hafif)

5.10 Error Boundary Yok
     Tüm proje genelinde React Error Boundary bileşeni yok.
     Beklenmedik render hatası tüm uygulamayı beyaz ekranla çökertiyor.
     Çözüm: components/ErrorBoundary.js oluştur:
       class ErrorBoundary extends React.Component {
         state = { hasError: false };
         static getDerivedStateFromError() { return { hasError: true }; }
         render() {
           if (this.state.hasError) return <ErrorScreen />;
           return this.props.children;
         }
       }
     App.js'te NavigationContainer'ı sar.

5.11 Hardcoded Türkçe Metinler (Çeviri Eksik)
     Dosyalar: components/ListView.js, components/ListViewTv.js
     Örnekler: "Diğer Listeler", "Liste Bulunamadı", "Kapat", "Liste Oluştur"
     Sorun: İngilizce seçildiğinde Türkçe metin görünüyor.
     Çözüm: const { t } = useLanguage() ile translations/index.js'e ekle.


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  BÖLÜM 6 — 📱 UX / KULLANICI DENEYİMİ SORUNLARI
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

6.1  Bildirim Sistemi Devre Dışı
     Dosya: screens/tabs/SettingsScreen.js satır ~290-296
     Sorun: Bildirimler toggle'ı disabled={true}. Reminder sistemi var
            ama push bildirim gönderilmiyor. Kullanıcı özelliği görüyor
            ama kullanamıyor → güven kaybı.
     Çözüm: expo-notifications + Firebase Cloud Messaging entegrasyonu

6.2  Boş Toggle'lar — İşlevsiz Özellikler
     Dosya: screens/tabs/SettingsScreen.js satır ~191-231
     Sorun: "Verileri İndir" ve "Düşük Veri Modu" toggle'ları
            onValueChange={() => {}} — tamamen boş.
     Çözüm: Ya gerçekten implement et ya da "Yakında" etiketi ile gizle.

6.3  Splash Screen Sabit 2 Saniye
     Dosya: App.js satır ~479-489
     Sorun: setTimeout ile tam 2 saniye bekleniyor.
            Yavaş cihazda auth bitmeden ana ekrana geçilebilir.
            Hızlı cihazda 2 sn gereksiz bekletme.
     Çözüm: useAuth().loading tamamlanana kadar splash göster.

6.4  Chat Ekranında Tarih Ayırıcısı Yok
     Dosya: screens/ChatScreen.js
     Sorun: Mesajlar arasında "Bugün", "Dün", "3 Nisan" gibi ayırıcı yok.
            Uzun sohbetlerde navigasyon zorlaşıyor.

6.5  TvShowScreen'de Animasyon Eksik
     Dosya: screens/tabs/TvShowScreen.js
     Sorun: MovieScreen'deki giriş animasyonu TvShowScreen'de yok.
            Tab geçişlerinde tutarsız UX.

6.6  Search Ekranında Debounce Eksik
     Dosyalar: screens/search/MovieSearch.js, TvShowSearch.js
               screens/search/SearchFriendsScreen.js (var)
     Sorun: Her karakter girişinde API isteği gidiyor.
            SearchFriendsScreen'de 500ms debounce var ama
            MovieSearch/TvShowSearch'te kontrol edilmeli.

6.7  Arkadaşların Listelerini Görme Özelliği Kırık Olabilir
     Dosya: screens/tabs/profile/FriendsListScreen.js
     Sorun: listVisible subcollection yapısına geçildi ama
            arkadaşın profilini okurken hâlâ eski format bekleniyor olabilir.
     Kontrol: FriendsListScreen.js satır 79-94 arası okuma mantığı
              yeni map formatını destekliyor mu?

6.8  Avatar Yükleme Cloudinary'e Gitmiyor Mu?
     Dosyalar: screens/tabs/profile/UploadToCloudinary.js
               context/ProfileUiContext.js
     Sorun: 56 local avatar var (assets/avatar/) ama aynı zamanda
            Cloudinary upload kodu da var. İki sistem paralel mi çalışıyor?
            Kullanıcı kendi fotoğrafını yükleyebiliyor mu, yoksa sadece
            önceden tanımlı 56 avatar mı kullanabiliyor?


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  BÖLÜM 7 — 📦 BUNDLE SIZE / PAKET SORUNLARI
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

7.1  @react-navigation/stack Gereksiz (~200 KB)
     Kullanılıyorsa native-stack ile değiştir.
     Kullanılmıyorsa kaldır.

7.2  react-native-markdown-display Büyük (~150 KB)
     Sadece Gemini yanıtları için kullanılıyorsa daha hafif alternatif dene.

7.3  react-native-appwrite Override — Temizle
     Dependencies'de yok ama overrides'ta var → karışıklık.

7.4  expo-image Kullanılmıyor
     React Native'in varsayılan <Image> bileşeni disk cache yapmıyor.
     expo-image: otomatik disk cache, blurhash placeholder, daha az bellek.
     Kurulum: npx expo install expo-image
     Değiştir: import { Image } from 'expo-image'
     Kazanç: Poster yoğun uygulamada %60-80 daha hızlı resim yükleme.

7.5  Lottie JSON Boyutları
     Dosyalar: LottieJson/ klasörü
     Sorun: snow.json, play.json, loading15.json vb. kaynak boyutları
            büyük olabilir. Uygulamaya gömülü.
     Çözüm: lottie-optimizer ile küçült veya gerekli olmayan animasyonları sil.


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  BÖLÜM 8 — 🏗️ MİMARİ GELİŞTİRMELER
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

8.1  MovieContex + TvShowContex → Custom Hooks
     Mevcut: Her şey tek dev context'te (29+23 state)
     Hedef:
       hooks/
         useMovieTrends.js    (trend state + fetch)
         useMovieBests.js     (best state + fetch)
         useMovieOscar.js     (oscar state + fetch, 7 gün cache)
         useMovieProviders.js (provider state + fetch)
         useMovieGenres.js    (genre state + fetch)
         useTvTrends.js       ...
     Faydası: Ekran sadece ihtiyacı olan hook'u çekiyor,
              diğer state değişimleri o ekranı etkilemiyor.

8.2  Merkezi API Servis Katmanı Yok
     Mevcut: axios her context/ekranda ayrı ayrı, headers tekrarlanıyor
     Hedef:
       services/
         tmdb.js  → axios instance, baseURL, Authorization header
         movies.js → film endpoint'leri (getTrending, getBests, ...)
         tv.js    → dizi endpoint'leri
     Faydası: Tek yerden değişiklik, retry/timeout merkezi yönetim.

8.3  Repository Katmanı Yok (Firestore)
     Mevcut: doc/getDoc/setDoc/deleteDoc çağrıları her ekranda dağınık
     Hedef:
       repositories/
         listsRepo.js      → addToList, removeFromList, getList
         watchedTvRepo.js  → addShow, addSeason, removeShow
         notesRepo.js      → createNote, updateNote, deleteNote
         remindersRepo.js  → addReminder, removeReminder
         statsRepo.js      → incrementStat, getStats
     Faydası: Test edilebilir, bakımı kolay, veri katmanı UI'dan ayrı.

8.4  Provider Ağacı Derinliği — App.js
     Mevcut: 12+ iç içe Provider (Christmas tree)
     Hedef: compose() utility:
       const AppProviders = ({ children }) => (
         <AppSettingsProvider>
           <LanguageProvider>
             <ThemeProvider>
               ...
             </ThemeProvider>
           </LanguageProvider>
         </AppSettingsProvider>
       );

8.5  MovieDetail.js ve TvShowsDetails.js Bölünmeli
     Mevcut: 1.839 + 1.837 satır, her şey tek dosyada
     Tamamlanan: MovieCastItem.js + MovieVideoItem.js oluşturuldu
     Kalan:
       MovieDetail/
         MovieHeader.js    (poster, başlık, puan, türler)
         MovieActions.js   (listeye ekle, favori, izlenecek)
         MovieSimilar.js   (benzer filmler FlatList)
         MovieComments.js  (yorum bölümü)

8.6  TypeScript Geçişi — Kademeli
     Mevcut: Tamamen JavaScript
     Önerim: Yeni dosyaları .tsx olarak yaz. Context'lere önce tip ekle.
     Neden: Runtime tip hataları (undefined.map gibi) compile time'da yakalanır.
            Özellikle 29-state MovieContex'te tip hataları çok olası.


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  BÖLÜM 9 — ✨ HENÜZ YAPILMAMIŞ ÖZELLİKLER
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

9.1  Push Bildirim (expo-notifications)
     Reminder eklenince yerel bildirim planlansın.
     Film/dizi çıkış tarihi yaklaşınca hatırlatma gelsin.

9.2  Gemini AI Entegrasyonu — Önerim Bölümü
     ChatModal.js'te temel Gemini entegrasyonu var ama
     "İzlediklerime göre öneri" özelliği yok.
     User'ın watchedMovies + genres verisiyle Gemini'ye öneri sor.

9.3  Deep Link Desteği
     seelogd://movie/123 formatında link paylaşımı.
     Arkadaşa film linki at, direkt açılsın.

9.4  expo-image ile Görsel Cache
     Şu an her scroll'da posterler yeniden yükleniyor.
     expo-image: otomatik disk cache + blurhash placeholder.

9.5  WatchParty / Grup İzleme
     Arkadaşlarla aynı filmi "eş zamanlı" izleme modü.
     Firestore realtime + Chat entegrasyonu ile mümkün.

9.6  Profil Paylaşım Kartı
     "Bu yıl X film izledim, en çok Y türü seviyorum" gibi
     görsel kart, react-native-view-shot + Share API.

9.7  Expo OTA Güncelleme
     expo-updates ile mağaza güncellemesi gerektirmeden
     bug fix dağıtımı. eas.json'da preview channel var.

9.8  Gelişmiş Filtreleme — Arama Ekranı
     Yıl, tür, dil, puan aralığı filtreleri.
     TMDB /discover endpoint zaten destekliyor.

9.9  Uygulama İçi Puanlama
     TMDB puanı yerine kendi puanı (1-10).
     İstatistik ekranlarına "ortalama puanım" eklenir.

9.10 Karıştır Modu — Ne İzlesem?
     Rastgele öneri + SwipeCard modülü zaten var.
     Sol swipe: geç, sağ swipe: izleneceklere ekle.


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  BÖLÜM 10 — 🔒 GÜVENLİK ÖZETİ
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ❌ KRİTİK  | TMDB API key hardcoded (AppSettingsContext.js)
  ❌ KRİTİK  | Gemini API key hardcoded (ChatModal.js)
  ❌ KRİTİK  | Firestore Security Rules uygulanmadı
  ⚠️ ORTA    | Firebase config açık (firebase.js — env var'a taşı)
  ⚠️ ORTA    | Adult content filtresi sadece API parametresi,
               render tarafında da filtre olmalı
  ✅ İYİ     | Firebase Authentication ile kullanıcı doğrulaması var
  ✅ İYİ     | onSnapshot'lar cleanup ediliyor (memory leak yok)
  ✅ İYİ     | Kullanıcı verisi uid bazlı ayrılıyor


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  BÖLÜM 11 — 📊 ÖNCELIK SIRALI AKSİYON PLANI
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

┌─────────────────────────────────────────────────────────────────────────────┐
│ AŞAMA 1 — HEMEN (1-2 gün, yayın öncesi zorunlu)                           │
├─────────────────────────────────────────────────────────────────────────────┤
│ [ ] API key'leri .env'e taşı, hardcoded'ları sil            (Bölüm 2.1)   │
│ [ ] Firebase Security Rules'u yayınla                       (Bölüm 2.3)   │
│ [ ] console.log'ları __DEV__ ile koru ya da transform-remove-console      │
│     ekle (29 dosya)                                         (Bölüm 5.3)   │
│ [ ] Error Boundary ekle                                     (Bölüm 5.10)  │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ AŞAMA 2 — BU HAFTA (1 hafta, performans kazanımı)                         │
├─────────────────────────────────────────────────────────────────────────────┤
│ [ ] ListsScreen + ListsViewScreen + ListsTvShowScreen'den   (Bölüm 4.4)   │
│     kendi listener'larını kaldır → ListStatusContext'ten oku               │
│ [ ] TvShowContex'teki eski watchedTv listener'ı kaldır     (Bölüm 4.3)   │
│ [ ] Refresh fonksiyonlarına try/catch/finally ekle          (Bölüm 3.9)   │
│ [ ] expo-image kur, Image bileşenlerini değiştir            (Bölüm 7.4)   │
│ [ ] ProfileStatsContext scaleValues → useRef                (Bölüm 3.3)   │
│ [ ] Hardcoded TR metinler → LanguageContext                 (Bölüm 5.11)  │
│ [ ] Splash auth-bazlı yap                                   (Bölüm 6.3)   │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ AŞAMA 3 — BU AY (2-3 hafta, büyük mimari)                                 │
├─────────────────────────────────────────────────────────────────────────────┤
│ [ ] Migration script: root-doc arrays → subcollection migrate, sil        │
│ [ ] UserStats migration: mevcut watchedMovies'ten hesapla  (Bölüm 4.9)   │
│ [ ] MovieDetail / TvShowsDetails bileşen parçalama          (Madde 28)    │
│ [ ] MovieContex → custom hooks (en az Oscar + Collection)  (Bölüm 8.1)   │
│ [ ] services/tmdb.js merkezi axios instance                 (Bölüm 8.2)   │
│ [ ] Push bildirim (expo-notifications)                      (Bölüm 9.1)   │
│ [ ] @react-navigation/stack kaldır                          (Bölüm 5.6)   │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ AŞAMA 4 — BACKLOG (1-2 ay, uzun vade)                                     │
├─────────────────────────────────────────────────────────────────────────────┤
│ [ ] Repository katmanı (Firestore soyutlaması)              (Bölüm 8.3)   │
│ [ ] TypeScript kademeli geçiş                               (Bölüm 8.6)   │
│ [ ] watchedTv subcollection SeasonDetails.js entegrasyonu                 │
│ [ ] Gemini AI öneri özelliği                                (Bölüm 9.2)   │
│ [ ] WatchParty, Deep Link, Paylaşım Kartı                   (Bölüm 9.x)   │
└─────────────────────────────────────────────────────────────────────────────┘


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  BÖLÜM 12 — KISA ÖZET (TEK BAKIŞTA)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  GENEL SKOR    : 6.5 / 10
  GÜVENLİK      : 4 / 10  (API key'ler açık, Security Rules yok)
  PERFORMANS    : 6 / 10  (subcollection geçişleri yapıldı, listener sayısı hâlâ fazla)
  KOD KALİTESİ : 6 / 10  (typo'lar, büyük dosyalar, console.log'lar)
  VERİTABANI    : 7 / 10  (iyi subcollection mimarisi, eski array'ler temizlenmedi)
  UX            : 7 / 10  (güzel animasyonlar, bazı özellikler yarım)
  MİMARİ        : 6 / 10  (context'ler çok büyük, repository katmanı yok)

  EN İYİ YÖNLER:
  • Firestore subcollection migrasyonu büyük ölçüde tamamlandı
  • O(1) liste status index
  • Dual-layer TMDB önbellek
  • Comment ve Chat optimizasyonları
  • SwipeCard gesture sistemi
  • Profil context parçalanması

  EN KRİTİK SORUNLAR:
  1. Hardcoded API key'ler (TMDB + Gemini)
  2. Firestore Security Rules yok
  3. MovieContex/TvShowContex çok büyük (29+23 state)
  4. Aynı Firestore belgesine birden fazla listener
  5. 1.800+ satır dosyalar (MovieDetail, TvShowsDetails)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  Rapor Tarihi: 2026-05-17
  Analiz Kapsamı: Tüm kaynak dosyalar (68 ekran, 14 context, 18 bileşen)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
