# Active Context

## 🔥 Şu Anki Odak

- Reanimated 4 sonrası hata giderme
- Firestore veri yapısının alt koleksiyonlara ayrılması
- Performans optimizasyonu (lazy load, pagination)
- Tema ve dil sisteminin yeniden yapılandırılması

## ⚙️ Yeni Kararlar

- Context yönetimini Zustand’a taşımak (plan aşamasında)
- `ProfileScreenProvider` içinde veri önbellekleme
- Firestore sorgularında `limit` + `startAfter` kullanımı

## 🧠 Öğrenilenler

- `onSnapshot` gereksiz yerlerde kullanıldığında performans düşüyor
- Cache ile API istekleri %40 azaltılabiliyor
- `useMemo` ve `useCallback` UI yanıt süresini belirgin iyileştiriyor
